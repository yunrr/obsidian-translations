{
  "schemaVersion": 1,
  "metadata": {
    "plugin": "better-search-views",
    "version": "2.0.0",
    "title": "Better Search Views",
    "description": "Outliner-like breadcrumb trees for search, backlinks and embedded queries ",
    "language": "zh-cn",
    "supportedVersions": "0.2.2",
    "author": ""
  },
  "dict": {
    "main.js": {
      "ast": [],
      "regex": [
        {
          "source": "Notice(`File ${path} does not exist`)",
          "target": "Notice(`文件 ${path} 不存在`)"
        },
        {
          "source": "Notice(`Failed to open file ${path}. Can't scroll to line ${line}`)",
          "target": "Notice(`打开文件 ${path} 失败。无法滚动到行 ${line}`)"
        },
        {
          "source": "Notice(\n      `Better Search Views: ${message}. Please report an issue with the details from the console attached.`,\n      errorTimeout\n    )",
          "target": "Notice(\n      `Better Search Views: ${message}。请报告问题并附上控制台的详细信息。`,\n      errorTimeout\n    )"
        },
        {
          "source": ".log(\"Ignoring invalid or overlapping range: \" + (\"\" + JSON.stringify(range)",
          "target": ".log(\"忽略无效或重叠的范围：\" + (\"\" + JSON.stringify(range)"
        },
        {
          "source": ".log(\"Ignoring invalid range: \" + JSON.stringify(range)",
          "target": ".log(\"忽略无效范围：\" + JSON.stringify(range)"
        },
        {
          "source": ".log(\"End range automatically set to the max value of \" + max)",
          "target": ".log(\"结束范围自动设置为最大值\" + max)"
        },
        {
          "source": ".log(\"Invalid range: \" + JSON.stringify(range)",
          "target": ".log(\"无效范围：\" + JSON.stringify(range)"
        },
        {
          "source": ".log(\"Skipping whitespace only range: \" + JSON.stringify(range)",
          "target": ".log(\"仅跳过空格范围：\" + JSON.stringify(range)"
        },
        {
          "source": ".log('Searching with expression \"' + regexp + '\"')",
          "target": ".log(' 用表达式搜索 \"' + regexp + '\"')"
        },
        {
          "source": ".log('Searching with expression \"' + regex + '\"')",
          "target": ".log(' 用表达式搜索 \"' + regex + '\"')"
        },
        {
          "source": ".log(\"Starting to mark with the following ranges: \" + JSON.stringify(ranges)",
          "target": ".log(\" 开始标记以下范围： \n\" + JSON.stringify(ranges)"
        },
        {
          "source": ".log('Removal selector \"' + sel + '\"')",
          "target": ".log('移除选择器 \"' + sel + '\"')"
        },
        {
          "source": ".error(`${message}. Reason:`, error)",
          "target": ".error(`${message}。原因：`, error)"
        }
      ]
    }
  }
}