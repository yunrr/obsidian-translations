{
  "schemaVersion": 1,
  "metadata": {
    "plugin": "obsidian-hotkeys-for-specific-files",
    "version": "2.0.0",
    "title": "Hotkeys for specific files",
    "description": "Set hotkeys for specific files and open them just with your keyboard.",
    "language": "zh-cn",
    "supportedVersions": "1.4.1",
    "author": ""
  },
  "dict": {
    "main.js": {
      "ast": [],
      "regex": [
        {
          "source": ".setButtonText(\"Add\")",
          "target": ".setButtonText(\"添加\")"
        },
        {
          "source": ".setButtonText(\"Create\")",
          "target": ".setButtonText(\"创建\")"
        },
        {
          "source": ".setName(\"Prefer existing panes\")",
          "target": ".setName(\"优先使用现有窗格\")"
        },
        {
          "source": ".setName(\"Add command to open file in Hover Editor\")",
          "target": ".setName(\"添加命令以在悬浮编辑器中打开文件\")"
        },
        {
          "source": ".setName(\"Add new commands (required after changes)\")",
          "target": ".setName(\"添加新命令（更改后需要重新添加）\")"
        },
        {
          "source": ".setName(\"Create new text field\")",
          "target": ".setName(\"创建新文本字段\")"
        },
        {
          "source": ".setName(\"Create new text field with moment format\")",
          "target": ".setName(\"使用 moment 格式创建新文本字段\")"
        },
        {
          "source": ".setName(\"File to open with command (with moment format)\")",
          "target": ".setName(\"使用命令打开的文件（带 moment 格式）\")"
        },
        {
          "source": ".setName(\"File to open with command\")",
          "target": ".setName(\"使用命令打开的文件\")"
        },
        {
          "source": ".setDesc(\n      \"Turn on to prefer existing panes and only open it in the current pane if the files isn't opened already.\"\n    )",
          "target": ".setDesc(\n      \"开启此选项将优先使用现有窗格，若文件未打开，则在当前窗格中打开。\"\n    )"
        },
        {
          "source": ".setDesc(\n      \"Needs the Hover Editor plugin to be installed and enabled.\"\n    )",
          "target": ".setDesc(\n      \"需要安装并启用悬浮编辑器插件。\"\n    )"
        },
        {
          "source": ".setDesc(\n      \"To remove already added commands, you have to reload Obsidian (or disable and enable this plugin) \"\n    )",
          "target": ".setDesc(\n      \"要移除已添加的命令，您需要重新加载 Obsidian（或禁用并重新启用此插件）\"\n    )"
        },
        {
          "source": ".setDesc(\n        \"Include file extension(e.g. .md)! Is excluded from moment format.\"\n      )",
          "target": ".setDesc(\n        \"请包含文件扩展名（如 .md）！不包含在 moment 格式中。\"\n      )"
        },
        {
          "source": ".setDesc(\"Include file extension(e.g. .md)!\")",
          "target": ".setDesc(\"请包含文件扩展名（如 .md）！\")"
        },
        {
          "source": ".setPlaceholder(\"Directory/file.md\")",
          "target": ".setPlaceholder(\"目录/文件.md\")"
        },
        {
          "source": ".appendText(\"For syntax, refer to \")",
          "target": ".appendText(\"语法请参考 \")"
        },
        {
          "source": ".appendText(\n              \"Your current syntax looks like this: \"\n            )",
          "target": ".appendText(\n              \"您当前的语法如下：\"\n            )"
        }
      ]
    }
  }
}